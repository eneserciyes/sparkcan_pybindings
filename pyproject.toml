[build-system]
requires = ["scikit-build-core>=0.10.0", "pybind11>=2.12", "cmake>=3.22", "ninja"]
build-backend = "scikit_build_core.build"

[project]
name = "sparkcan_py"
version = "0.1.0"
description = "Python bindings for SparkFlex/SparkMax (sparkcan)"
requires-python = ">=3.10"

